---
title: Quick Start
description: Connect a portal, pick a collector, and run a script.
---

import { Aside } from '@astrojs/starlight/components';

## 1. Add portal

Run **LMDA: Add Portal** and enter your portal hostname (e.g. `acme.logicmonitor.com`) and Bearer token. Credentials are stored securely by VS Code and are not saved in your settings file.

## 2. Select collector

Run **LMDA: Select Collector**. Groovy scripts run on Linux or Windows collectors; PowerShell requires a **Windows** collector.

## 3. Open a script

Use the walkthrough sample, open a local `.groovy` or `.ps1` file, or load a script from **LogicModules** in the sidebar.

## 4. Run

Press `Cmd+Alt+R` / `Ctrl+Alt+R`, click the editor **Play** icon, or run **LMDA: Run Script**. Output appears in the **LMDA Composer** panel.

<Aside type="note">Set **Mode** in the status bar to validate auto-discovery or collection output in Problems.</Aside>

<img src="/lmda-composer-vscode-docs/screenshots/run-script.png" alt="Run script" style="border-radius: 0.5rem; margin: 1.5rem 0;" />

## Push to portal

Edit a loaded module script, then use **Push to Portal** and review the diff before publishing.

<img src="/lmda-composer-vscode-docs/screenshots/push-to-portal.png" alt="Push to portal" style="border-radius: 0.5rem; margin: 1.5rem 0;" />
